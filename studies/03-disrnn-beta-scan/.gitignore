# Curated analysis outputs (beta_scan_summary.json/.csv, beta_scan_final_grid.csv,
# the two fig_*.png, and the reports) ARE committed — so the results render
# directly in the repo tree. Only caches/scratch are ignored.

# W&B download dirs + intermediate per-run pulls (re-derivable from W&B)
analysis/_cache_*.json
analysis/wandb/
analysis/artifacts/
analysis/live_pull_*.csv

# Python bytecode
__pycache__/
*.pyc

# Legacy names from the pre-normalization exploratory layout
backfill_sparsity_metrics.csv
backfill_sparsity_metrics_stepwise.csv
