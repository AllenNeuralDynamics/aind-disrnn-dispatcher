# gru-stage1

**Stage 1 · GRU**

**What differs.** static (no drift). Grid: {50,100,200,300}×{none,scalar}×hid{16,64}.

**Result.** subject param R² 0.91–0.96 @D4

**W&B.** project `embedding_recovery`, group `gru-stage1@<launch_id>`, sweep [`mfuaz3ki`](https://wandb.ai/AIND-disRNN/embedding_recovery/sweeps/mfuaz3ki).

See the study README Variants index and `analysis/reports/INDEX.md` (r1 GRU ladder / r2 disRNN) for the cross-variant synthesis.
