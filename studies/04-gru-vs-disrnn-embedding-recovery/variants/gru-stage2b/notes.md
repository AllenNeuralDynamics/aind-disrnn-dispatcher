# gru-stage2b

**Stage 2b · GRU**

**What differs.** strong+non-monotonic drift, tail held-out. Grid: {none,scalar} N=200.

**Result.** BASELINE FLIP: GRU>0.987 vs baseline 0.939

**W&B.** project `embedding_recovery`, group `gru-stage2b@<launch_id>`, sweep [`c838tjrw`](https://wandb.ai/AIND-disRNN/embedding_recovery/sweeps/c838tjrw).

See the study README Variants index and `analysis/reports/INDEX.md` (r1 GRU ladder / r2 disRNN) for the cross-variant synthesis.
