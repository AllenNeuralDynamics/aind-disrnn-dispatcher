# baseline-losscounting-stage4b

**Stage 4b · baseline LC**

**What differs.** per-session-switch reference. Grid: N=200.

**Result.** baseline eval-LL ref

**W&B.** project `embedding_recovery`, group `baseline-losscounting-stage4b@<launch_id>`, sweep [`b3m2un3h`](https://wandb.ai/AIND-disRNN/embedding_recovery/sweeps/b3m2un3h).

See the study README Variants index and `analysis/reports/INDEX.md` (r1 GRU ladder / r2 disRNN) for the cross-variant synthesis.
