# gru-stage4a

**Stage 4a · GRU**

**What differs.** family mixture (QL/CTT/LC). Grid: {4,8,16}×{none,scalar}.

**Result.** family decoding 100%

**W&B.** project `embedding_recovery`, group `gru-stage4a@<launch_id>`, sweep [`93pci64o`](https://wandb.ai/AIND-disRNN/embedding_recovery/sweeps/93pci64o).

See the study README Variants index and `analysis/reports/INDEX.md` (r1 GRU ladder / r2 disRNN) for the cross-variant synthesis.
