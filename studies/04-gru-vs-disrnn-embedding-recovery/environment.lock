# Pinned analysis env for studies/04-gru-vs-disrnn-embedding-recovery/.
#
# The load-bearing line is the wrapper pin below: _meta.build_meta() parses
# `aind-disrnn-wrapper.git@<sha>` from this file and stamps it into every
# recovery_summary.json as _meta.wrapper_git_sha. That sha is the version of
# aind-disrnn-wrapper that (a) trained the ladder and (b) whose model-agnostic
# analysis/stage4b_recovery.py produced the recovery grids this study reads
# (extract_subject_embeddings_from_params + compute_session_conditioned_context_dataframe).
#
# The analysis side (analysis/recovery_report.py) needs only stdlib + numpy +
# pandas + matplotlib; it reads the committed grid CSVs and (when re-pulling)
# talks to W&B over raw GraphQL, not the wandb SDK. The wrapper pin documents the
# PRODUCER of the metrics, not an import dependency of the analysis.
#
# Producer (training + recovery-metrics) wrapper commit:
-e git+https://github.com/AllenNeuralDynamics/aind-disrnn-wrapper.git@f8764dc276038df1c95f4b4bfa3a487bc7d0f6b4#egg=aind_disrnn_wrapper

# Analysis-side runtime (numpy + pandas + matplotlib is all recovery_report.py
# imports beyond stdlib). Pinned loosely — figures/JSON insensitive to minor versions.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
