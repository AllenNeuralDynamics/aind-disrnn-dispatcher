# Curated analysis outputs (recovery_summary.json/.csv, the *_grid.csv inputs, the
# three fig_*.png, and the reports) ARE committed — so results render directly in
# the repo tree. Only caches/scratch are ignored.

# W&B download dirs + intermediate per-run pulls (re-derivable from W&B)
analysis/_cache_*.json
analysis/wandb/
analysis/artifacts/
analysis/live_pull_*.csv

# Python bytecode
__pycache__/
*.pyc
